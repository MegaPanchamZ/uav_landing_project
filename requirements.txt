# Core dependencies for UAV Landing Detector - Single Class Implementation
opencv-python>=4.8.0
numpy>=1.21.0
onnxruntime>=1.15.0  # Use onnxruntime-gpu for GPU support

# Optional for model training/conversion (not needed for inference only)
# torch>=2.0.0
# torchvision>=0.15.0

# Essential utilities
tqdm>=4.64.0
